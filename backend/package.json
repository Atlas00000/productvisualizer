{
  "name": "backend",
  "version": "1.0.0",
  "description": "Express API server for 3D Product Customizer",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "seed": "node scripts/seed.js",
    "seed:clear": "node -e \"require('./utils/seedDatabase').clearDatabase().then(() => process.exit(0)).catch(() => process.exit(1))\"",
    "test:db": "node scripts/test-db.js",
    "test": "echo \"No tests specified yet\" && exit 0"
  },
  "keywords": [
    "express",
    "api",
    "3d",
    "product",
    "customizer"
  ],
  "author": "",
  "license": "ISC",
  "packageManager": "pnpm@10.10.0",
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^17.2.0",
    "express": "^4.18.2",
    "jsonwebtoken": "^9.0.2",
    "mongoose": "^8.16.4"
  },
  "devDependencies": {
    "nodemon": "^3.1.10"
  }
}
